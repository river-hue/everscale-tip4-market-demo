{
  "name": "everscale-tip4-market-demo",
  "version": "0.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "test-remote": "locklift test --disable-build --network dev --config locklift.config.js --test test",
    "test": "locklift test --disable-build --network local --config locklift.config.js --test test",
    "build": "export TVM_LINKER_LIB_PATH=$PWD/TON-Solidity-Compiler/lib/stdlib_sol.tvm && locklift build --config locklift.config.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@broxus/contracts": "^1.0.4",
    "bignumber.js": "^9.0.2",
    "locklift": "^1.5.3"
  }
}
